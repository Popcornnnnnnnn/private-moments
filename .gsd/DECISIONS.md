# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | workflow alignment discussion 2026-04-30 | workflow | Persistent project workflow and documentation source of truth | .gsd is the structured source for current facts, requirements, decisions, and milestone state; docs remains the stable human-facing documentation set. | The project will continue to evolve across iOS, server, admin, sync, and storage; separating structured execution facts from stable operator/product docs reduces drift. | Yes | human |
| D002 | workflow alignment discussion 2026-04-30 | workflow | Work planning mode | Use lightweight continuous maintenance by default, with milestone/slice planning required for high-risk domains. | Small UI and documentation changes should stay fast, while sync, schema migration, storage, backup, security, and cross-device work need explicit boundaries and evidence. | Yes | human |
| D003 | workflow alignment discussion 2026-04-30 | documentation | Documentation responsibilities | Keep docs single-purpose: PRD for product intent, technical design for system design, runbook for operations, integration guide for API use, handoff for current state, workflow for process. | Single-purpose documents reduce repeated content and make stale sections easier to detect. | Yes | human |
| D004 | workflow language alignment 2026-04-30 | documentation | Language policy for human-facing documentation | Human-facing documentation under docs/ should be primarily Chinese, while command names, API identifiers, code symbols, filenames, and app UI copy may stay in English where that is the product or technical source language. | The project owner reads and maintains the stable docs directly, while .gsd and AGENTS primarily serve agent/tooling workflows; separating language expectations reduces friction without forcing code/API terms into awkward translations. | Yes | human |
| D005 | M001 discussion | product | Moments product positioning for management and writing features | Treat Moments as a private expression space, not a management database or writing editor. | The user's north star is “一个没有观众的生活表达空间”: features should preserve lightweight expression, private feed-like browsing, and no audience pressure rather than optimizing for archiving or structured writing. | Yes | human |
| D006 | M001 discussion | ux | Date navigation product shape | Add lightweight month-first, optional-day timeline jump inside the toolbar calendar menu; do not add a Calendar tab, archive management view, empty-date picker, or daily counts. | This solves growing-content navigation while preserving the main timeline as a quiet feed and avoiding a database/statistics feel. | Yes | human |
| D007 | M001 discussion | ux | Text input assistance boundary | Support plain-text list continuation only for bullet and numbered lists; do not render Markdown or add heading, bold, quote, or link-preview formatting. | List continuation removes small input friction without turning Moments into a Markdown editor or long-form writing tool. | Yes | human |
| D008 | M003 comments discussion 2026-04-30 | planning | Supersede old detail-only comments milestone | Treat the previous M002 detail-only private comments direction as superseded/reference-only; create M003 for WeChat Moments-style feed comments and preserve the current old-M002 removal direction. | The user explicitly rejected the old detail-centric shape and wants comments to live in the main timeline; keeping a new milestone avoids hidden restoration of old assumptions. | Yes | human |
| D009 | M003 comments discussion 2026-04-30 | product | Comments product shape | Add single-user `Comments` as a private main-timeline interaction, not a multi-user, public, or family-sharing social system. | The desired social feel is the interaction pattern, while the product still has no real audience or second author. | Yes | human |
| D010 | M003 comments discussion 2026-04-30 | ux | Feed comment interaction model | Comments are read, created, expanded, collapsed, and deleted in the main timeline; Moment detail keeps existing detail/edit/media behavior but does not manage comments. | The user wants a WeChat Moments-like flow: tap a feed comment button, use a bottom input bar, see comments under the moment, and long-press to delete. | Yes | human |
| D011 | M003 comments discussion 2026-04-30 | ux | Timeline comment density and controls | Every moment shows a quiet comment icon; comments show real counts, latest-two previews, `View all N comments`, `Show less`, full multiline text, lightweight English relative time, and no empty state. | This gives comments enough feed presence while controlling timeline height and avoiding preview-less hidden data. | Yes | human |
| D012 | M003 comments discussion 2026-04-30 | ux | Comment input and draft handling | Use a bottom input bar with target summary, multiline input, Return for newline, `Send` button, 500-character max, send-success input closure with scroll-to-moment-bottom feedback, and discard confirmation for non-empty drafts on close or target switch. | These choices match the requested feed-first comment flow while preventing accidental wrong-target sends or draft loss and making successful sends visibly land on the newest comment. | Yes | human |
| D013 | M003 comments discussion 2026-04-30 | data-sync | Comment persistence and sync model | Implement comments as independent local-first entities with server `comments`, iOS `local_comments`, `create_comment`, and `delete_comment`; no `update_comment` in the first version. | Independent entities preserve durability, idempotent operations, soft delete, and recovery better than embedding comments into post payloads. | Yes | human |
| D014 | M003 comments discussion 2026-04-30 | data-sync | Comment lifecycle and cursor safety | Reject comments for missing/deleted parent posts, cascade/hide comments on parent deletion, do not emit per-comment deletes for parent deletion, short-circuit unsynced create-then-delete locally, and fail comment apply without advancing cursor if parent is missing. | Comments are subordinate to moments, and sync recovery must not swallow comment changes or create orphan UI. | Yes | human |
| D015 | M003 comments discussion 2026-04-30 | product | Comment content boundaries | Comments are plain multiline text only, max 500 characters, with Markdown-like text shown literally; no media, rich text, link previews, replies, likes, mentions, author names, editing, or copy/selection in the first version. | The feature should support quick follow-up expression without becoming a second editor or social feedback system. | Yes | human |
| D016 | M003 comments discussion 2026-04-30 | search | Comment-aware search behavior | Apply existing filters first, then match moment text or comment text; if comments match, prioritize up to two matching comments in preview and lightly emphasize matching comment rows without changing real comment counts. | Search results should be explainable without bypassing current filters or turning the timeline into a dedicated search results view. | Yes | human |
| D017 | M003 comments discussion 2026-04-30 | verification | Comments verification bar | Require migration/recovery verification plus real iPhone UAT for main-timeline comments when feasible; diagnostics may show comment operation types/counts but not comment body text. | The risky parts are schema/sync recovery and real-device keyboard/gesture behavior, and verification must avoid leaking private content. | Yes | human |
| D018 | M004 audio/video discussion 2026-04-30 | product | Audio/video moment scope | Add audio and video as first-class moment media, like images, and keep comments text-only. Moments may be text-only, image, video, audio, or text plus one allowed media kind. | The user wants voice/video as expression content attached to a moment, not as comment attachments or a social messaging layer. | Yes | human |
| D019 | M004 audio/video discussion 2026-04-30 | product | Media cardinality and mixing | First version allows only one media kind per moment: up to 9 images, or 1 video, or 1 audio recording. Do not mix images, video, and audio in one moment. | One media kind keeps timeline layout, editing, upload recovery, and playback state tractable while preserving the main use cases. | Yes | human |
| D020 | M004 audio/video discussion 2026-04-30 | video | Video capture, processing, and display | Video is selected from Photos, limited to 2 minutes, rejected if longer, compressed immediately in composer to 720p H.264 MP4 with audio, and published only if iOS generates a usable poster image. Timeline shows poster plus play button; playback is fullscreen, foreground-only, and no speed controls. | This reuses the image-style library flow, controls file size, avoids first-version trimming UI, and preserves a quiet timeline. | Yes | human |
| D021 | M004 audio/video discussion 2026-04-30 | audio | Audio recording and playback | Audio is recorded inside composer as AAC M4A, up to 60 minutes, with background and lock-screen recording, pause/resume/stop, abnormal interruption draft recovery, pre-publish listen/rerecord/delete, timeline playback controls, persisted per-audio progress, 1x/1.5x/2x speed, and background/lock-screen playback. | Long voice notes need stronger recording and playback continuity than short media while still remaining a single moment attachment. | Yes | human |
| D022 | M004 audio/video discussion 2026-04-30 | ux | Composer and edit media replacement | Composer and edit allow switching media type only after confirmation; confirmation removes the previous media and preserves text and occurred time. Edit supports cross-type replacement and deletion under the one-media-kind rule. | The behavior is consistent between new and edit flows and avoids accidental loss of selected media or recordings. | Yes | human |
| D023 | M004 audio/video discussion 2026-04-30 | sync-storage | Audio/video storage and recovery | Store only compressed video and poster on Mac, store recorded M4A for audio, do not preserve original video. Recovery automatically fetches video poster and audio metadata; full audio/video downloads on play, then remains in local cache until future cache cleanup. | This balances local-first playback with storage and Tailscale reliability, avoiding automatic full-video recovery. | Yes | human |
| D024 | M004 audio/video discussion 2026-04-30 | ux | Media failure and cache controls | Upload failures keep the moment locally visible with media-level Uploading or Upload failed plus Retry, and local playback remains available. On-demand download failures show inline media-card Retry. Settings Storage should include cleanup for re-downloadable audio/video caches. | Media errors should be visible and recoverable without interrupting timeline browsing or deleting user content. | Yes | human |
| D025 | M004 audio/video discussion 2026-04-30 | search-admin | Search, filter, and Admin boundaries | Search remains text/comment-only; Filter menu adds Audio and Video. Admin shows audio/video type, size, status, and poster/thumbnail diagnostics but does not play private audio/video. | This keeps discovery explainable and avoids turning Admin into a content consumption surface. | Yes | human |
| D026 | speech transcription follow-up 2026-04-30 | search-sync | Supersede text/comment-only search for audio/video transcripts | Add best-effort iOS on-device speech transcription for audio/video media; sync transcript text as media metadata and include it in iOS/server/Admin search, while avoiding cloud transcription fallback. D033 supersedes the original direct timeline snippet display. | The user wants voice-to-text discovery for recorded media, but the app should preserve privacy, offline-friendly behavior, and a quiet feed rather than becoming a transcript reader or external AI service client. | Yes | human |
| D027 | transcription visibility follow-up 2026-04-30 | ux-recovery | Transcript location and fallback visibility | Transcript output has no separate page. After D033, successful transcript text is accessed through the `Summary` bottom sheet fallback rather than direct timeline display; old uploaded `not_requested` media and older `No speech%` failures are locally retried through Chinese-first recognizer fallback before settling. | The user needs to find transcript output, but the feed should not duplicate AI summaries or show video subtitle-like transcript text. | Yes | human |
| D028 | M005 AI media summaries discussion 2026-04-30 | product | First AI feature shape | Start with a quiet tool-style AI summary for audio/video moments, not AI comments, random personas, or automatic background analysis. | The user wants AI first as an organizing assistant; starting with summaries gives clear utility and avoids making the timeline feel watched or noisy. | Yes | human |
| D029 | M005 AI media summaries discussion 2026-04-30 | ai-architecture | AI execution boundary | The Mac server calls an external AI API; iOS never stores or sends provider API keys directly. | The user explicitly prefers external API over local model deployment, and centralizing calls on the server keeps provider config, privacy controls, and retry behavior in one place. | Yes | human |
| D030 | M005 AI media summaries discussion 2026-04-30 | ux | AI summary display model | Main timeline shows only `Summary` / lightweight status; ready summaries open in a bottom sheet. Audio without a ready summary can fall back to transcript text; video never shows transcript text as visible subtitle/body. | This preserves the timeline simplicity constraint while making generated summaries and audio transcript fallback easy to inspect when requested, without turning video posts into subtitle cards. | Yes | human |
| D031 | M005 AI media summaries discussion 2026-04-30 | data-sync | AI output persistence model | Store AI summaries as generated metadata attached to media, with provider/model/prompt version and status; do not write summaries into comments or mutate post/transcript text. | AI output is derived material and should be explainable, removable, regeneratable, and sync-recoverable without altering the source moment. | Yes | human |
| D032 | M005 AI media summaries discussion 2026-04-30 | product | Future AI persona layer deferral | Defer AI reviewer/persona/private-society behavior until after the tool-style summary layer works. | The playful AI direction is promising, but it has a different product feel and should build on a stable AI artifact model instead of being mixed into the first implementation. | Yes | human |
| D033 | summary/transcript UX follow-up 2026-05-01 | ux | Summary entry owns transcript fallback | Supersede direct timeline transcript snippets for audio/video. The timeline keeps only a `Summary` entry or lightweight status; opening it shows ready AI summary only when available. Audio without ready summary can show transcript fallback with Generate/Retry; video without ready summary does not show transcript text and uses transcript only as generation input. | AI summary and transcript text were duplicative in the feed, and video should not show transcript as an external subtitle. Keeping summary behind one entry preserves feed simplicity while still allowing raw transcript fallback where it is useful for voice memos. | Yes | human |
| D034 | server-side media summary follow-up 2026-05-01 | ai-architecture | Supersede iOS local transcription for AI summaries | New audio/video posts do not run iOS Speech transcription or show transcript fallback. The Mac server starts summary generation after media upload, sends the stored media file to an external transcription API, sends the internal transcript to the summary API, and syncs only ready generated summary metadata back to iOS. The timeline shows `Summary ready` only after a ready summary exists; the comment action stays as the right-side feed interaction. | Local transcription quality was incomplete enough to undermine summaries, and the UI looked cleaner when transcript/status placeholders disappeared. Keeping transcript internal to the Mac server makes AI quality depend on the external provider while preserving a quiet feed. | Yes | human |
| D035 | local transcription follow-up 2026-05-01 | ai-architecture | Use Mac-local transcription plus external summary API | Supersede the external transcription API leg of D034. Mac server transcribes uploaded audio/video locally with `mlx-whisper`, then sends only the internal transcript to the configured external summary API. | The configured localhost provider did not provide usable audio transcription/audio-input behavior, while local Mac transcription produced usable text and keeps provider credentials server-only. This preserves the quiet ready-only iOS UI without relying on iPhone Speech transcription. | Yes | human |
| D036 | AI summary diagnostics follow-up 2026-05-01 | sync-observability | Diagnose AI summary progress in Settings and fix remote-only summary pull | Keep Timeline silent until `Summary ready`, but use Settings > Storage & Diagnostics plus sync cursor checks to tell whether missing summaries are stuck in transcribing, summarizing, provider failure, or client cursor staleness. iOS must pull server-originated AI summary changes even when no local work is pending. | The real iPhone had `lastSyncCursor=173` while the Mac had summary server changes through version 194, so the app could look broken even though server summaries were ready. The diagnostic surface should remain outside the main timeline. | Yes | human |
| D037 | AI summary readability follow-up 2026-05-01 | ai-ux-data | Render summaries from structured document blocks | New summaries use `media-summary-v2` with `documentTitle`, `oneLiner`, and `documentBlocks` rendered by iOS as Markdown-like native UI with headings, subheadings, paragraphs, bullet/numbered lists, and `AI suggested` callouts. Existing summaries stay compatible and only regenerate into the new model when requested. Search participation was later expanded by D039, and new generation was later hardened to `media-summary-v3` by D056. | Long spoken notes need a scannable, layered reading experience without storing arbitrary Markdown as the source of truth. A block model keeps rendering safe and stable while allowing adaptive structure, fact/reflection separation, and clearly labeled inferred suggestions. | Yes | human |
| D038 | AI summary regenerate lifecycle follow-up 2026-05-01 | ux-data | Regenerate preserves old summary until replacement succeeds | Manual regeneration continues after the Summary sheet is dismissed. Timeline shows lightweight `Regenerating` while the request is in flight and `Summary failed` if the replacement fails. The server keeps the previous summary fields during transcribing/summarizing/failed states and replaces them only when a new result is ready. | Regenerate should feel like a background refresh, not a destructive clear-and-wait action. The user needs feedback after leaving the sheet and should not lose a useful old summary because a new AI call failed. | Yes | human |
| D039 | search and AI hardening follow-up 2026-05-01 | search-observability | Local fuzzy search and lightweight summary pipeline hardening | iPhone Timeline search includes post text, comments, AI summary generated metadata, and historical transcript metadata with lightweight fuzzy matching. Filters are AND-composable across content type, month, favorite, commented, needs-sync, and match source when a query exists; filter state is not persisted. AI summary jobs stay as in-memory background work but add stage/elapsed logs, one transient retry, and Settings diagnostics age/retry hints. | This improves retrieval and debuggability without adding a server search rewrite, a durable job queue, noisy timeline progress UI, or new backup/security scope. | Yes | human |
| D040 | v0.1 close-out discussion 2026-05-02 | release-ops | Internal RC before public open source | Treat the project as ready for v0.1 internal candidate cleanup, not ready for immediate public release. Add a non-destructive local setup script and release/open-source checklists first; require license choice, history secret scan, security/privacy notes, and backup/restore/export closure before public release. | The product feels usable, but open-source publication has different risks than personal use: private data, secret history, unclear license, AI transcript privacy, and setup friction must be made explicit before a public repo exists. | Yes | human |
| D041 | Share Extension capture-entry discussion 2026-05-03 | capture-ux | Share Sheet import stays thin and composer-owned | Add an iOS Share Extension named `Save to Moments` for external capture. The extension only stages supported shared items into the App Group inbox and opens Moments; the main app composer remains responsible for editing, image order, media preparation, publish, local database writes, upload, and sync. | This removes capture friction without duplicating core publish logic in an extension sandbox or crowding the main timeline with new entry controls. | Yes | human |
| D042 | media upload failure investigation 2026-05-03 | media-storage-observability | Harden media upload with temp-file finalization and stage logs | `/api/v1/media/upload` writes incoming multipart files to hidden temp files, atomically renames only after the full stream is received, removes only temp files on failure, and logs `media.upload_started`, `media.upload_received`, `media.upload_completed`, and `media.upload_failed` with safe metadata/error codes. | The real iPhone hit `ERR_STREAM_PREMATURE_CLOSE`, leaving posts created without media and stale server write fds. Atomic finalization prevents partial files from becoming committed media, while structured logs make Tailscale/client disconnects distinguishable from server write failures. | Yes | agent |
| D043 | ATS sync failure investigation 2026-05-03 | iOS-networking | Keep Debug HTTP fallback explicit and prefer Tailscale Serve HTTPS | The iOS Debug app keeps `NSAllowsArbitraryLoads` as the plain-HTTP fallback for private LAN/Tailscale development, while runbook guidance prefers Tailscale Serve HTTPS for real-device Server URL. Do not rely on `NSAllowsLocalNetworking` to cover Tailscale `100.x` addresses. | A real iPhone Sync Now request to `http://100.114.41.22:3210` failed before reaching the server with App Transport Security requiring a secure connection. Tailscale `100.x` was not reliably treated as local networking, and relying on HTTPS avoids ATS ambiguity without hard-coding a personal IP. | Yes | agent |
| D044 | M006 smart tags discussion 2026-05-03 | tags-product | Tags use a primary/topic model | Add synced tags with primary tags for expression type and topic tags for concrete content. Default primary tags are `日记`, `想法`, `学习整理`, `情绪`, `碎碎念`, and `复盘`; topic tags are dynamic, flat, canonicalized, and alias-aware. | This keeps the top-level taxonomy small and stable while still allowing specific retrieval such as `大语言模型`, `强化学习`, or `高斯概率分布`. | Yes | human |
| D045 | M006 smart tags discussion 2026-05-03 | tags-ai | AI tags are audio-only first-version generated metadata | AI automatic tags apply only to new audio moments, reuse the audio summary pipeline, apply once on first ready summary, do not require confirmation, do not regenerate with summary regeneration, and do not backfill historical audio. Manual tags remain available for all moment types. | The user wants AI tags to be asynchronous and trusted without odd confirmation popups, but also wants the first version scoped tightly to voice notes rather than all media/text. | Yes | human |
| D046 | M006 smart tags discussion 2026-05-03 | tags-ux | Timeline and Day Review show optional primary tag only | Timeline and Calendar Day Review tag display are controlled by `Show tags in Timeline`, show at most the primary tag, and do not disable tags when hidden. `synced` success status should disappear from timeline; only abnormal sync states remain, with right-side ordering primary tag, favorite star, abnormal sync status. | This preserves the quiet review surfaces while still making tags visible when useful, and prevents successful sync badges from competing with tag and favorite metadata. | Yes | human |
| D047 | M006 smart tags discussion 2026-05-03 | tags-management-sync | Tags are first-class synced vocabulary and assignments | Store and sync tag vocabulary separately from post tag assignments, with tag types, aliases, archive/restore, usage counts, source/confidence metadata, and deterministic conflict rules. iPhone Settings manages tags; Mac Admin is diagnostics-only. | Tags are long-lived retrieval metadata and must survive sync/reinstall, support alias cleanup, and avoid turning Admin into a private content management UI. | Yes | human |
| D048 | M006 smart tags discussion 2026-05-03 | tags-verification | Smart Tags requires milestone planning and real iPhone UAT | Implement Smart Tags as a new milestone with schema, sync, AI pipeline, iOS UI, Settings management, shared docs, and real iPhone UAT. Do not treat it as a quick fix. | The feature crosses database migrations, sync recovery, AI generated metadata, search/filter behavior, and timeline UI; the core value depends on the real asynchronous audio pipeline. | Yes | human |
| D049 | M006 smart tags implementation 2026-05-03 | tags-sync | Topic merge is a first-class sync operation | Add `merge_tag` with source topic as `entityId` and target topic in payload. Server moves active assignments, preserves the source name as target alias when possible, archives the source tag, and emits assignment/alias/tag server changes. | A local-only archive-plus-alias approximation would hide old assignments instead of preserving search/filter semantics. Merge must be recoverable and deterministic across devices. | Yes | agent |
| D050 | M006 smart tags follow-up 2026-05-03 | tags-ux-ai | Tag color editing defaults to presets and AI tag failures must be explainable | Settings > Tags defaults primary tag color editing to a preset palette, keeps 6 low-saturation defaults plus clearer medium/high-saturation choices, and still provides a custom HEX input for exact colors. AI tag generation first asks the summary prompt for `suggestedTags`, falls back to a focused tag-only provider call when tags are empty, and logs safe `ai.tags_processed` metadata with confidence counts and skipped reason. | Raw color strings should not be the primary Settings interaction, but exact custom colors are still useful as an advanced path. Empty AI tag results also looked like a UI bug, so the pipeline needs a stronger fallback and privacy-safe evidence for whether tags were absent, low-confidence, blocked, or applied. | Yes | agent |
| D051 | edit media UI follow-up 2026-05-03 | edit-media-ux | Edit Moment renders existing media by kind | Edit Moment should only use the sortable thumbnail grid for image media. Existing audio/video media should render as audio/video previews and should disable image add/camera controls while present. | The image grid tried to load audio files through `UIImage`, producing a misleading placeholder below `Use Camera`. The edit screen must preserve one-media-kind semantics and avoid implying that audio is an image. | Yes | agent |
| D052 | appearance mode follow-up 2026-05-03 | settings-ux | App appearance override is local-only | Settings adds an Appearance section with `System`, `Light`, and `Dark`; the value is stored locally in `UserDefaults`, applies immediately through SwiftUI `preferredColorScheme`, and is not synced across devices. | A third-party iOS app cannot change the whole device appearance, and appearance is a device-context preference. Keeping it local avoids schema/sync scope while giving the user direct control inside Moments. | Yes | human |
| D053 | smart tags archived-delete follow-up 2026-05-03 | tags-sync-ux | Archived custom tags can be permanently deleted | Settings > Tags allows permanent deletion only after a tag is archived and only for non-default tags. Deletion syncs as `delete_tag`, removes active assignments and aliases, emits `tag_deleted`, and frees the normalized name for recreation under another tag type. | Archive/restore preserves the original tag type, which is wrong for accidental Topic-vs-Primary creation. Permanent delete is the clean recovery path, but keeping it behind Archived prevents casual destructive edits from the active tag list. | Yes | human |
| D054 | smart tags topic-count and batch-management follow-up 2026-05-03 | tags-ai-ux | Keep AI topic tags sparse and support batch tag cleanup | AI topic suggestions should be conservative: short audio prefers one topic, with multiple topics only for clearly separate high-confidence themes. Settings > Tags batch edit supports Topic bulk archive/merge and Archived bulk restore/delete. | Short voice notes were at risk of over-tagging, while tag cleanup becomes tedious once AI creates several topic tags. Sparse AI output plus batch cleanup preserves lightweight organization without creating a tag wall. | Yes | human |
| D055 | AI title auto-insert follow-up 2026-05-03 | ai-ux-sync | Audio AI title may write back as a limited Markdown heading | Supersede the absolute no-mutation part of D031 only for new audio moments: when the first ready audio AI summary has a valid `documentTitle` and the moment text has no leading `# ` or `## ` title, iOS may insert `## <title>` at the top of `post.text` through a dedicated `insert_ai_title` sync operation. Summary regeneration does not overwrite it, historical audio is not backfilled, Settings > Feature Modules can disable future inserts, and Timeline/Detail render only line-leading `# ` / `## ` headings while Composer/Edit remain plain text. | Voice-note titles are useful as part of the scannable moment itself, but the feed must not become a Markdown editor or have AI overwrite user writing. A narrow operation avoids marking the moment as manually Edited and keeps the generated title synced without copying summary bodies into logs. | Yes | human |
| D056 | AI title generation hardening 2026-05-03 | ai-ux-sync | Recognizable audio summaries should produce short titles | New summaries use `media-summary-v3`: for any recognizable non-empty audio/transcript note, the provider is instructed to return a `documentTitle` of at most 40 characters, and the server derives a safe fallback title from `oneLiner` if the provider still returns a blank or overlong title. Null titles remain allowed only for content-free, unintelligible, silence/noise cases. | A real-device UAT note produced a ready summary and AI tags but no title because the v2 prompt allowed `documentTitle: null` whenever the model judged a title unnecessary. That made title auto-insert look broken even though sync was behaving correctly. | Yes | human |
| D057 | iOS localization discussion 2026-05-03 | i18n-ux | App language is a local UI preference | Add an iOS App Language preference with `System`, `English`, and `简体中文`. New installs default to `System`; existing private installs should migrate to `English` so the current owner's UI does not unexpectedly switch. The setting applies immediately and is stored locally in `UserDefaults`, not synced to the server. | Language is a device-context display preference like appearance. Keeping it local avoids sync/schema scope, while immediate application makes the setting understandable and testable. | Yes | human |
| D058 | iOS localization discussion 2026-05-03 | i18n-scope | First localization pass covers iOS user-visible UI only | Localize the iOS app's user-visible surfaces, including Timeline, Composer, Detail/Edit, Settings, Tags, Summary, Search/Filter, alerts, and date labels. Do not localize Mac Admin, server logs, API names, code symbols, or human-facing project docs as part of the first app-language milestone. | Partial localization inside the iOS app would feel inconsistent, but expanding into Admin/docs/server would widen the milestone without improving the main phone experience. | Yes | human |
| D059 | iOS localization discussion 2026-05-03 | i18n-tags-search | Default primary tags are one synced tag with localized display names | Default primary tags keep one stable tag identity and may display Chinese or English names depending on App Language. English default names are `Diary`, `Thoughts`, `Study`, `Mood`, `Random`, and `Review`. Custom primary tags, topic tags, and aliases are user content metadata and are not translated. Search/filter should let default primary tags match both Chinese and English display names. | Tags must remain stable retrieval metadata; language should change the label, not split usage counts, AI assignments, sync identity, or historical search behavior. | Yes | human |
| D060 | iOS localization discussion 2026-05-03 | ai-i18n | AI output language is independent from App Language | Add an AI Language preference with `Auto`, `Chinese`, and `English`, defaulting to `Auto`. `Auto` chooses the dominant input language for AI summary/title; Chinese audio with English technical terms should still summarize in Chinese, while English-dominant input should summarize in English. App UI language does not force AI output language. | AI summary/title are derived content, not interface chrome. They should respect the source material and the user's content workflow rather than the language chosen for buttons and settings. | Yes | human |
| D061 | M007 iOS localization implementation 2026-05-03 | i18n-implementation | Use an app-local localization layer for immediate language switching | Implement `AppLocalization.swift` with `AppLanguageMode`, `AILanguageMode`, an `appLanguage` SwiftUI environment value, and an `L10n` dictionary instead of relying only on static Apple string catalogs. Localized default-primary tag names and bilingual default-tag search are handled in iOS display/search helpers; AI language is passed as request metadata to the Mac summary pipeline. | The requirement is immediate in-app switching plus a local-only preference. A small app-local layer lets Settings, dates, tags, search/filter labels, and generated-summary language controls update without an app restart or server preference schema. | Yes | agent |
| D062 | M008 Calendar discussion 2026-05-03 | calendar-product | Promote Calendar to a bottom-tab review mode | Supersede the no-Calendar-tab part of D006. Add a bottom `Calendar` / `日历` tab beside Timeline. Calendar is a primary review/navigation overview, while full reading remains in Timeline. Bottom tabs are only Timeline and Calendar; Settings stays a low-frequency toolbar entry and Calendar has no compose entry. | The user now wants stronger回看 ability than a toolbar menu can provide, but still wants Timeline to remain the continuous reading surface and does not want Calendar to become a second publishing path. | Yes | human |
| D063 | M008 Calendar discussion 2026-05-03 | calendar-navigation | Calendar day selection drives Timeline day filtering | Superseded by D067. Original decision: Calendar defaults to month view. Tapping a day switches to Timeline with a temporary day filter active chip; clearing the chip restores the full Timeline. Normal tab switching preserves Timeline scroll position. The old Timeline `Jump to date` toolbar calendar icon should be removed rather than kept as a parallel date entry. | A bottom Calendar tab should own date review. Day filtering made the selected date focused and visible, but later UAT made Day Review the more useful primary tap target. | No | human |
| D064 | M008 Calendar discussion 2026-05-03 | calendar-visuals | Month view uses quiet heatmap density plus small media hints | Calendar first screen is a month grid with continuous month navigation, horizontal swipe plus arrows, a lightweight `Today` / `今天` affordance, low-saturation paper-like heatmap color based on moment count, and at most two media type icons per day. Today gets a subtle ring; future dates are faded and not tappable; empty months show normal empty calendars without explanatory copy. | This gives an intuitive overview of lived-time density without turning the app into a KPI dashboard or cluttered content wall. | Yes | human |
| D065 | M008 Calendar discussion 2026-05-03 | calendar-scope | Calendar v1 is derived local UI only | Calendar v1 reads only local Timeline data, supports its own lightweight filters for media type and favorite, follows App Language/locale first weekday, includes VoiceOver semantics, and does not add schema, sync, Mac statistics, compose flows, holidays/lunar dates, week/month AI summaries, or README screenshots in the first version. | The first value is date回看, not a new data pipeline. Keeping it derived and local lowers risk and preserves offline consistency with Timeline. | Yes | human |
| D066 | M008 Calendar discussion 2026-05-03 | calendar-preview | Day preview is a secondary long-press affordance | Superseded by D067. Original decision: tapping a day goes directly to Timeline day filtering. Long-pressing a day visibly presses the date cell, opens a quiet preview with all moments from that day, renders lightweight Markdown text, shows the primary tag, shows all image thumbnails for image moments, omits video preview/poster, and lets each preview item open that exact moment detail inside the preview sheet so Back returns to the day preview. | The long-press model made the useful review path too hidden once Calendar became a core回看 surface. | No | human |
| D067 | M008 Calendar Day Review follow-up 2026-05-04 | calendar-navigation-ux | Calendar day tap opens Day Review first | Calendar date tap opens a full Calendar-stack `Day Review` page with light press feedback, date/weekday/count/media composition header, no-card intra-day timeline rows, optional primary tags following `Show tags in Timeline`, lightweight `#` / `##` rendering, all image thumbnails as uniform small grid cells, inline audio playback, video as a non-playing type/duration hint, and detail pushes that return to the same Day Review scroll path. `Timeline` is a top-right secondary action that clears the Day Review navigation path and applies the existing Timeline day filter. | Calendar use is primarily “what happened that day,” so the convenient tap target should open the day review; Timeline positioning remains available without duplicating the default path. | Yes | human |
| D068 | M008 Calendar Day Review follow-up 2026-05-04 | audio-ux | Audio playback uses a global waveform voice bar | Replace the old play-button + slider audio card with a reusable waveform voice bar across Timeline, Detail/Edit, and Day Review. The bar uses finer deterministic pseudo-waveform bars, tap to play/pause, drag to seek, a right-side lightweight speed capsule menu, compact Timeline/Day Review density, a slightly taller Detail density, and a play control aligned to the waveform region rather than the whole text-bearing row. Full playback completion still clears saved progress and returns to the initial state. | The old slider was functional but visually heavy. A voice-bar model keeps seek/rate capability while fitting the app's quieter interface language. | Yes | human |
| D069 | UAT navigation/tag follow-up 2026-05-04 | tags-audio-calendar-ux | Hidden tags, in-app audio pause, and Day Review scroll memory | `Show Tags in Timeline` also hides Moment Detail tag display and disables that detail-page tag editing entry; tag data, search/filter, Settings management, sync, and AI tags still continue. Audio pauses on active in-app interface changes such as tab switches, detail pushes, settings/composer/summary/gallery/video presentation, or popping review/detail screens, but must keep playing during backgrounding or lock screen. Day Review stores the visible moment id per calendar date and restores that position when returning to the app or rebuilding the view. | The user expects the tag visibility switch to simplify all reading surfaces, not leave a detail edit affordance behind; audio should not leak across in-app browsing contexts, but background playback remains useful; Day Review should behave like a remembered reading page rather than restarting at the top. | Yes | human |
| D070 | Smart Tags primary repair 2026-05-04 | tags-i18n-ai | Default primary tag canonical names are server-protected | Default primary tag IDs are immutable system identities. The server coerces those IDs back to canonical Chinese names, `isDefault=true`, and `aiUsableAsPrimary=true` on seed and `upsert_tag`; iOS treats known default IDs as defaults even if a stale local row says otherwise. AI primary matching accepts both canonical Chinese names and localized English display names, and summary generation falls back to tag-only classification whenever primary is missing, not only when all tags are empty. | Real-device color editing sent localized/default tag rows as ordinary `upsert_tag` payloads with `isDefault=false`, changing server canonical names to English and causing Chinese AI primary suggestions to fail matching while topic tags still applied. The pipeline must protect stable taxonomy data and explain primary skip reasons separately from topic application. | Yes | agent |
| D071 | Composer paste images follow-up 2026-05-04 | composer-media-ux | Pasted images become composer media drafts | New Moment text input may intercept real clipboard image data and append it to the existing image draft grid, subject to the same 9-image limit and one-media-kind rule as Photos/Camera. Plain text paste remains normal, Markdown image syntax is not parsed, and Edit Moment does not gain image paste in this first pass. | The user wants paste-to-media convenience without turning the editor into rich text or a Markdown image renderer. Reusing the existing composer media pipeline preserves preview, draft persistence, publish, upload, and sync behavior. | Yes | human |
| D072 | Calendar Review density/statistics follow-up 2026-05-04 | calendar-review-ux | Calendar uses dynamic review density and lightweight monthly/day filters | Calendar month heatmap scales density against the visible month's busiest day, date cells may show exact moment count labels, and the navigation bar top-trailing toolbar includes a lightweight Month Stats sheet. Calendar month filters affect the month grid and stats, while tapping a day opens the full Day Review; Day Review has its own horizontal chips for media, favorite, comments, and summary filters, uses 24-hour times, and adds light period separators. | The user often has 10-20+ moments per day, so the old fixed 4+ heat bucket collapsed busy days into the same visual state. Review surfaces should reveal daily rhythm and improve lookup without turning the main Timeline into a statistics dashboard. | Yes | human |
| D073 | Calendar Review filter/statistics refinement 2026-05-04 | calendar-review-ux | Calendar removes standalone Summary dimensions and makes review filters interactive | Supersede the summary-filter portion of D072. Day Review chips are multi-select OR filters across Photos, Audio, Video, Favorites, and Comments; tapping All clears the selection. Summary is not a standalone Day Review filter or Month Stats count because Audio is the meaningful user-facing category and should normally imply summary availability. Month Stats has no Close button, supports swipe-down dismissal, and its bars/busiest day/content rows act as lightweight drill-down or Calendar filter shortcuts. | The user found single-select filters too limiting, the Close button redundant for a sheet, the static stats surface stiff, and Summary semantically duplicative with Audio in this product flow. | Yes | human |
| D074 | Markdown editor discussion 2026-05-04 | composer-edit-markdown-ux | Moment body editing uses a lightweight rich Markdown layer | Supersede D007 and the Composer/Edit plain-text part of D055 for Moment body text only. Store post text as Markdown source, but Composer and Detail Edit render a Typora/Obsidian-like lightweight editor for line-leading H1, H2, and unordered lists. Heading markers are shown only while the cursor is on the heading line; unordered lists render as bullets without exposing `-`. A keyboard accessory toolbar provides H1, H2, unordered-list toggle, and Done. Pasted images still become media drafts rather than inline body content. Timeline, Detail view, and Day Review render H1/H2/list consistently, with Timeline using smaller heading scale. Comments remain plain text. | The user wants better drafting and AI-title readability without turning Moments into a full Markdown note app. Keeping Markdown as storage preserves sync/search/export simplicity, while limiting formatting to headings and unordered lists keeps the main timeline quiet and implementation tractable. | Yes | human |
| D075 | Markdown editor IME follow-up 2026-05-04 | composer-edit-markdown-ux | Markdown editor styling must be IME-safe and compact | The Composer/Edit Markdown editor must not rewrite `UITextView.textStorage` or externally replace text while an iOS input method has active marked text. The keyboard accessory formatting controls should stay visually compact as one H1/H2/list segmented control plus Done, not a row of loose toolbar buttons. | Chinese/Japanese/Korean input uses marked text while composing candidates; reapplying attributed text during that phase breaks candidate conversion and leaves raw pinyin/letters. Compact controls reduce visual weight while keeping quick formatting available. | Yes | assistant |
| D076 | Markdown list rendering follow-up 2026-05-04 | composer-edit-markdown-ux | Editor list bullets are app-controlled, not system NSTextList markers | Composer/Edit unordered list source remains `- `, but the editor renders the dash as a small app-controlled bullet attachment and hides the following marker space. Do not depend on `NSTextList` inside editable `UITextView` for this lightweight Markdown layer. | `NSTextList` rendering in editable `UITextView` is visually unstable when the source marker is hidden; owning the bullet size and indent keeps the point aligned with the editor's quiet, compact visual language while preserving Markdown source. | Yes | assistant |
| D077 | Markdown list rollback and tag color batch follow-up 2026-05-04 | composer-edit-markdown-ux tags-ux | Markdown rich editing is headings-only; primary tag colors support batch edits | Supersede the unordered-list parts of D074-D076. Composer/Edit Markdown rendering and keyboard accessory controls now support only H1 and H2. Existing plain text list continuation remains available for ordinary text, but `- ` is no longer parsed or rendered as a Markdown bullet in Composer/Edit, Timeline, Detail, or Day Review. Settings > Tags keeps the existing primary-tag color palette and HEX input, expands presets with high-saturation standard colors, and Edit mode can select multiple primary tags to apply one color to all selected tags. | The editable bullet rendering was not reliable enough in UIKit to justify the complexity. H1/H2 solve the user's main drafting need, while batch primary color editing makes tag cleanup faster without adding rename or schema complexity. | Yes | human |
| D078 | Markdown editor accessory follow-up 2026-05-04 | composer-edit-markdown-ux | Markdown accessory omits Done | Composer/Edit Markdown keyboard accessory keeps only the compact H1/H2 segmented control and removes the `Done` button. Keyboard dismissal should rely on native gestures/navigation rather than adding a low-value toolbar action above the editor. | The user found the extra Done button unnecessary, and removing it makes the input chrome quieter while preserving the useful heading shortcuts. | Yes | human |
| D079 | market research 2026-05-05 | product-strategy | Position Private Moments as private social timeline rather than generic journal | Add `docs/MARKET-RESEARCH.md` as the durable market/competitor reference. The recommended public framing is a no-audience private social timeline with local-first capture, optional self-hosted archive, and quiet AI review; avoid positioning primarily as a better Day One, pure AI voice journal, or generic self-hosted journal. | Market research found strong adjacent competition in mature journal apps, AI voice journals, private memory apps, and self-hosted journals, while the distinct opportunity is the combination of feed-like private expression, multimedia moments, local ownership, Calendar review, comments-to-self, and restrained AI organization. | Yes | agent |
| D080 | v0.1 archive discussion 2026-05-05 | product-strategy | Optimize v0.1 for owner long-term self-use before public distribution | M009 targets the current iPhone + Mac server self-use loop. iOS standalone, App Store/TestFlight, public marketing, and open-source promotion are not part of this milestone. | Market research showed many adjacent products and high distribution cost; the owner chose to prioritize a private daily-use tool that is stable, recoverable, and trustworthy. | Yes | human |
| D081 | v0.1 archive discussion 2026-05-05 | backup-restore | Admin-managed restic backup with project-managed key | Backup/restore is managed through Mac Admin rather than raw CLI. The repository uses restic for deduplicated snapshots, but the project auto-generates and stores `.private-moments-restic-key` next to the repository so the user does not manage a password. | The owner wants long-term low-friction recovery and may place the repository in iCloud Drive, but does not want to risk forgetting a backup password. | Yes | human |
| D082 | v0.1 archive discussion 2026-05-05 | backup-restore | Restore is staged and promote is strongly confirmed | Restore writes to a new data directory first. Promote requires verification, maintenance mode, a pre-promote snapshot, strong confirmation, and rollback-aware switching. | Direct overwrite is too risky for a private archive; staged restore keeps recovery useful without making one wrong click destructive. | Yes | human |
| D083 | v0.1 archive discussion 2026-05-05 | sync-observability | Sync Health appears in both Mac Admin and iOS Settings | Sync Health should distinguish reachability, auth, cursor lag, outbox, media upload, missing media, AI summary state, and last successful sync; safe repair actions are allowed but destructive reset actions are out of scope. | The owner repeatedly needed to know where sync/AI/media had stalled, but Timeline should stay quiet. | Yes | human |
| D084 | v0.1 archive discussion 2026-05-05 | export-import | Export/import is migration-first rather than reading-first | Export uses JSON manifest/metadata as the authoritative source, media files as payload, and Markdown only as preview. Import from export is in scope for Phase B but only into a new/empty data directory, preserving archive identity and generated metadata while excluding auth/session/device runtime state. | The owner cares more about recovery and future import than reading exported files directly; a migration-first format avoids lossy Markdown as the source of truth. | Yes | human |
| D085 | v0.1 archive discussion 2026-05-05 | maintenance-ops | Archive operations use durable serial maintenance jobs and maintenance mode | Backup, restore, check, promote, export, import, and sync-health work should use durable `maintenance_jobs`; jobs run serially, and restore/promote enters maintenance mode to pause ordinary writes. | Long-running recovery work needs browser-refresh-safe state, no concurrent archive mutations, and privacy-safe diagnostics. | Yes | human |
| D086 | M009 implementation 2026-05-05 | backup-restore | Promote writes restart instructions rather than hot-swapping SQLite | Supersede the in-process active-directory switch part of D082 for v0.1. Promote preparation verifies the staged restore, enters maintenance mode, creates a pre-promote restic snapshot, and writes `archive/pending-promote.json` with the restored directory plus required `PRIVATE_MOMENTS_DATA_DIR` and `DATABASE_URL`; the operator then stops the server, updates env, and restarts. | Prisma keeps an open SQLite connection, so replacing the live database directory in-process is riskier than a staged restart handoff. The recovery path remains safe and explicit while avoiding a fragile hot swap. | Yes | agent |
| D087 | capture and local-only follow-up 2026-05-05 | sync-ux | Automatic Sync can be disabled as strict Local-only mode | Add an iOS `Automatic Sync` local setting. When off, the app does not run bootstrap/foreground/retry/follow-up sync, media upload, remote media download, or AI summary/tag network requests automatically; local creates/edits/comments/tags remain durable in the local SQLite outbox. Explicit `Sync Now` is still allowed and performs a user-initiated sync pass. | The user wants Moments to work as a pure local app when desired, without hidden attempts to connect to the Mac server. Keeping manual sync available preserves recovery and migration without confusing automatic background behavior. | Yes | human |
| D088 | capture and local-only follow-up 2026-05-05 | edit-sync | Pending moments are editable with local final state as source of truth | Pending, partial, failed, and synced moments can enter Edit Moment and Edit Tags. Edits apply locally immediately, create normal outbox operations, and the sync layer reconciles the latest local state once the Mac server is reachable. | A local-first app should not block editing because upload/sync has not finished. The visible iPhone state should be the user's current truth, while outbox ordering handles later server convergence. | Yes | human |
| D089 | capture and local-only follow-up 2026-05-05 | text-ux | Moment text has a detail-level Markdown source copy action | Add a low-noise copy action for non-empty moment text in the detail surface. It copies the stored Markdown source, not rendered plain text or rich text. | The body text is intentionally stored as limited Markdown source, and copying that source is predictable for reuse in notes, WeChat, Obsidian, or debugging. Timeline stays clean. | Yes | human |
| D090 | capture and local-only follow-up 2026-05-05 | capture-ux | Screenshot and WeChat article capture use Share Sheet first | Treat screenshot capture and WeChat article forwarding as `Save to Moments` Share Extension flows, not custom timeline controls. The extension keeps staging thin; the main Composer owns final edit/publish. WeChat/Safari web URLs should be preserved as tappable link-card style text in Moments so the user can jump back through the original URL when iOS/WeChat supports it. | Third-party apps cannot rely on Apple's Quick Note screenshot overlay slots. Share Sheet is the stable system integration point and already matches the project's thin-extension architecture. | Yes | human |
| D091 | capture and local-only follow-up 2026-05-05 | audio-editing-ux | Recording mixes with other device audio and editor stability outranks live styling | Audio recording should use a play-and-record session that allows mixing with music/navigation when possible, accepting that background sound may be captured. Composer/Edit Markdown styling must avoid repeated text-storage rewrites that cause long-text focus loss, flashing, or auto-scroll; stable typing and scrolling outrank Typora-like live styling. | The user's capture scenarios happen while navigating/listening/reading, so recording should not interrupt the current audio environment. The editor bug affects core writing, so H1/H2 styling must remain lightweight and IME/scroll safe. | Yes | human |
| D092 | network fallback follow-up 2026-05-05 | iOS-networking | Private fallback endpoint is build-local, not public project config | iOS may include an optional bundled fallback server URL through `PRIVATE_MOMENTS_FALLBACK_SERVER_URL`, sourced from ignored local config during private builds. Settings still shows the primary Server URL; sync, media upload/download, AI summary, login, and storage diagnostics try the bundled fallback only after network-level primary endpoint failures. Public docs and reusable code must not hard-code the owner's personal domain, tunnel UUID, or DNS target. | The project should remain Tailscale/private-VPN-first for open-source reuse, while the owner's local install benefits from a Cloudflare Tunnel fallback when iOS is connected to another VPN and Tailscale cannot stay active. Keeping the personal endpoint outside tracked files preserves that boundary. | Yes | human |
| D093 | M010 discussion 2026-05-05 | ai-review-product | Weekly Review is a generic Review artifact, not a Timeline moment by default | Build a generic `Review` system and deliver `Weekly Review` first. Manual generation uses rolling seven days from trigger time; optional Sunday-evening automatic generation is default off, quiet, and unpublished. Reviews live under Calendar -> Reviews; only explicit publish-as-moment can create a normal moment. | The user wants weekly review now but expects monthly/longer reviews later. Keeping reviews outside Timeline preserves feed simplicity and avoids AI-generated text silently entering the user's life stream. | Yes | human |
| D094 | M010 discussion 2026-05-05 | ai-review-ux | Review uses whole-period reading and low-weight anchors only for revisiting | AI should not attach evidence to every theme, keyword, or reflection. It should read the week as a whole with calm observation plus moderate encouragement. Only `Worth Revisiting` may include review anchors, and those anchors open inside the review context rather than jumping Timeline. | Per-claim evidence would overfit isolated moments and make the feature feel like an audit. The valuable source link is for returning to lived moments, not proving AI judgments. | Yes | human |
| D095 | branch/worktree workflow discussion 2026-05-06 | workflow-data-safety | Use worktrees for parallel development and keep main as the fixed integration line | `main` is the stable version line. Each feature branch gets one dedicated worktree where development, testing, building, packaging, and UAT happen. Feature server runs use isolated runtime data by default, and real iPhone installs from worktrees must preserve the existing app container unless a planned recovery flow explicitly says otherwise. | Codex threads become confusing when multiple branches share one working directory, and Private Moments data safety depends on keeping temporary code from accidentally writing live archive data, downgrading local schema, or clearing the iPhone app container. | Yes | human |
| D096 | diagnostics refresh follow-up 2026-05-06 | sync-observability | Storage diagnostics refresh is read-only, explicit buttons sync | Settings > Storage & Diagnostics refresh should load local storage and Mac admin status without starting a hidden full sync. `Sync Now`, `Pull Server Changes`, `Retry Uploads`, and `Re-download Missing Media` remain the explicit repair actions. The iOS API client remembers the last reachable endpoint and admin status uses short timeouts so Tailscale primary failures do not make the page or later sync attempts look stuck. | The diagnostics page repeatedly appeared to be "thinking" because opening it silently started `/sync`, then waited on an unreachable Tailscale primary before falling back. A diagnostic surface should explain state quickly and avoid turning every visit into background work. | Yes | human |
| D097 | Settings sync spinner follow-up 2026-05-06 | sync-observability | Background sync should not look like an endless manual action | Settings root `Sync Now` should show the spinning icon only for a user-tapped sync action, not for any background `isSyncing` state. Empty foreground/background `/sync` checks use a short timeout before trying fallback endpoints; larger recovery/local-change sync passes keep longer timeouts. | After D096, the real iPhone could still show the Settings sync button spinning because app bootstrap/foreground auto-sync started an idle `/sync` against an unreachable Tailscale primary. The UI must distinguish silent background checks from explicit user work, and fallback should happen quickly. | Yes | human |
| D098 | iOS media sync SQL repair 2026-05-07 | sync-recovery | Server-originated media inserts must preserve column/value parity | When applying `media_uploaded` for media that does not yet exist in the local iOS database, insert a complete `local_media` row including both `createdAt` and `updatedAt`. | A real-device sync hit SQLite `20 values for 21 columns` because the recovery insert listed 21 columns but omitted the final `updatedAt` value. The failed prepare prevented cursor advancement, so a fixed client can safely retry the same server change. | Yes | agent |
| D099 | quality gate maintenance 2026-05-07 | verification | Release readiness requires explicit UAT gates | Track true-device and human acceptance gaps in `docs/UAT-GATES.md`; `verify:uat-gates` reports open gates and `verify:release-gates` fails while any gate remains open. Automated build/test evidence cannot close a gate that explicitly requires real iPhone, Mac Archive, Share Extension, AI quality, or user confirmation. | Recent work had many "implemented but UAT pending" paths spread across handoff and milestone notes. A single machine-checkable gate list keeps release readiness honest without blocking day-to-day checkpoint commits. | Yes | human |
| D100 | admin direction follow-up 2026-05-07 | operations-ux | Daily operations should migrate toward iOS Settings | New settings, monitoring, diagnostics, and safe repair controls should default to iOS Settings / Diagnostics. Mac Admin remains for low-frequency Mac-local operations such as Archive backup/restore, staged promote, export/import artifacts, server logs, LaunchAgent/process state, filesystem paths, and permissions. | The owner rarely uses the Mac Admin UI in practice; the phone is the primary product surface. Keeping routine diagnostics on iOS reduces context switching while preserving Admin for operations that only make sense on the Mac. | Yes | human |
| D101 | admin migration start 2026-05-07 | operations-ux | Admin opens Archive first and keeps only a minimal Mac-local surface | Mac Admin should default to `Archive`, order tabs as `Archive / Overview / Posts`, and treat the minimal long-term surface as Archive/recovery, promote/export/import artifacts, runtime truth, maintenance jobs, server logs, and device emergency. Posts remains only for troubleshooting and test-data cleanup while it is migrated down to Debug/Emergency. | The user wants Admin usage to keep shrinking because computer-side admin is rarely used. Making Archive the default reinforces that Admin is a Mac-local recovery console, not the daily content or diagnostics center. | Yes | human |
| D102 | admin migration implementation 2026-05-07 | operations-ux | iOS shows read-only Mac operations while Admin hides top-level Posts | Settings > Storage & Diagnostics should show read-only Mac runtime, maintenance, and Archive status; Mac Admin top-level navigation should be limited to `Archive / Overview`; Posts should not be a visible content management entry point. Backup, restore, promote, export, and import execution remains Mac Admin only. | This moves daily status checks to the phone without making iOS responsible for dangerous Mac filesystem/recovery actions, and it keeps Mac Admin focused on recovery rather than content browsing. | Yes | agent |
| D103 | AI topic tag reuse follow-up 2026-05-07 | tags-ai | AI topic generation reuses the active topic vocabulary first | New media summaries use `media-summary-v4`. Summary and tag-fallback prompts include active topic tag canonical names plus aliases, and server-side AI tag application resolves suggestions to existing active topic tags by exact name, alias, or obvious narrow/broad containment before creating new topic tags. | The live tag vocabulary split near-identical concepts such as `HTTPS 中间人攻击` and `中间人攻击`. The system needs a reuse-first loop rather than treating every provider phrase as a new canonical topic. | Yes | agent |
| D104 | iOS tag merge sync repair 2026-05-07 | tags-sync | Apply post tag server changes by assignment ID before post/tag uniqueness | iOS `local_post_tags` upsert now first checks whether the incoming assignment ID already exists. If it exists, iOS removes any local `(postId, tagId)` row that would conflict, then updates the existing row by ID. The normal `(postId, tagId)` upsert path remains for new assignments. | A server-side `merge_tag` can move a post assignment from the source topic to the target topic while preserving the assignment ID. The live iPhone hit `UNIQUE constraint failed: local_post_tags.id` because the previous apply path only handled `(postId, tagId)` conflict and tried to insert a row whose ID already existed locally. | Yes | agent |
| D105 | Moment Detail tag display repair 2026-05-07 | tags-ui | Detail tag badges show full names and keep source metadata secondary | Moment Detail uses a dedicated tag badge instead of the Timeline single-line compact chip. Tag names can wrap within the available width, and the `Manual` / `AI` source label sits below the badge instead of sharing the same row. Timeline and Day Review keep their low-noise primary-chip behavior. | Detail is the place where full tag metadata should be inspectable. Reusing the Timeline chip caused long names like `Study...` or `RSA...` to truncate and source labels to compete for horizontal space. | Yes | agent |
| D106 | Moment Detail source label follow-up 2026-05-07 | tags-ui | Hide tag source labels in normal read mode | Moment Detail tag badges show only the tag name. Assignment source remains stored for sync, AI overwrite rules, and diagnostics, but normal reading does not display `AI` or `Manual` under each badge. | The detail surface should stay readable and tag-focused. Showing source labels under every chip adds noise once the user only wants to inspect the assigned tags. | Yes | human |
| D107 | Diagnostics sync button follow-up 2026-05-07 | sync-observability | Manual diagnostics sync stays tappable during background sync | Settings > Storage & Diagnostics keeps `Sync Now` and `Pull Server Changes` enabled for authenticated users even when `store.isSyncing` is true; only the local tapped action state disables the active button. If a sync is already running, `TimelineStore.syncNow` records `needsFollowUpSync` and returns. | Sync Health actions are recovery controls. When pending outbox/media or a fallback retry is already driving background sync, disabling the manual buttons makes the user lose the one control meant to recover from that state. | Yes | agent |
| D108 | Storage diagnostics hierarchy follow-up 2026-05-08 | operations-ux | Storage diagnostics uses progressive disclosure | Settings > Storage & Diagnostics should keep the first screen short: summary status, iPhone total, Mac reachability, and only active warning counts. Detailed local storage, Sync Health actions, Mac runtime, Mac Operations, AI summaries/token usage, and tag diagnostics belong in second-level pages. | Moving more daily operations into iOS made the diagnostics page too long. The phone should show what needs attention first and keep low-frequency evidence one tap away without returning routine diagnostics to Mac Admin. | Yes | agent |
| D109 | Pinned Moments design 2026-05-08 | timeline-ux | Pinned moments are a collapsed shortcut shelf, not a second feed | The unfiltered Timeline may show a top `Pinned` section for pinned moments. It defaults to a collapsed summary row, while pinned moments still remain in their ordinary chronological Timeline rows with a lightweight pin status marker. Their original `occurredAt` still governs Calendar, Day Review, search/filter results, review input, and detail identity. Tapping a pinned title opens the original moment detail. | The user wants fast access to a few important moments while keeping the top of Timeline compact, but pinning must not make moments disappear from lived-time browsing. A status marker makes the ordinary row state clear without adding permanent action chrome. | Yes | human |
| D110 | Pinned Moments design 2026-05-08 | pin-sync-data | Pin state is lightweight synced post metadata | Represent pinning as `Post.isPinned` plus nullable `pinnedAt`, synced through a dedicated `update_post_pin` operation and `post_pin_updated` server change. Do not implement pinning as tags, favorites, duplicated posts, generated reviews, or Admin-only metadata. | Pinning has stronger layout semantics than favorite but belongs to the same post identity. A dedicated field/operation keeps search, Calendar, export/import, and sync recovery explicit without overloading existing organization or AI artifact concepts. | Yes | agent |
| D111 | Pinned Moments grill-me 2026-05-08 | timeline-ux | Pinned hides during Timeline search and filters | Show the Pinned surface only on the main Timeline when no search, date, tag, favorite, commented, content-type, needs-sync, or match-source filter is active. Do not add a Pinned filter or pinned-only Timeline tab in the first implementation. | The user chose Pinned as a quiet default Timeline shortcut, not a competing view inside focused search/filter tasks. Hiding it during filters avoids mixing unrelated shortcuts into a constrained result set. | Yes | human |
| D112 | Pinned Moments grill-me 2026-05-08 | timeline-ux | Pinned defaults to a summary header with a three-item direct display limit | The default Timeline surface is a collapsed `Pinned · N` header. With one to three pinned moments, tapping the header expands/collapses title rows and remembers that local UI state. With more than three pinned moments, tapping the header opens a bottom sheet containing the full pinned title list. | The user wants the top of Timeline to stay small by default, and specifically chose a summary row after more than three pinned moments. A local expand preference keeps the small case convenient without syncing UI state. | Yes | human |
| D113 | Pinned Moments grill-me 2026-05-08 | pinned-sheet-ux | Pinned sheet owns list navigation and full detail behavior | The more-than-three pinned sheet uses an internal navigation stack. Selecting a pinned row pushes the normal full `MomentDetailView` inside the sheet, and Back returns to the pinned list. Sheet rows use context menu unpin; they do not show permanent unpin buttons. | The user wants to browse multiple pinned moments from the sheet while preserving full detail capabilities such as edit, delete, favorite, tags, and pin/unpin. | Yes | human |
| D114 | Pinned Moments grill-me 2026-05-08 | pin-actions-ux | Pin actions live in low-noise menus | Moment Detail exposes Pin/Unpin inside a top `More` menu. Ordinary Timeline rows expose Pin/Unpin through long-press/context menu. Pinned sheet rows expose Unpin through context menu. Do not add permanent Timeline row pin buttons or reuse the favorite swipe action. | Pin is important but low-frequency. Menus keep Timeline visually quiet and avoid competing with the existing favorite swipe pattern. | Yes | human |
| D115 | Pinned Moments grill-me 2026-05-08 | pin-sync-data | Pin is independent local-first metadata | Pin and Favorite are independent. Pending, failed, partial, and synced moments can all be pinned/unpinned locally; operations enter the outbox and sync later. Deleted moments disappear from pinned surfaces without a separate unpin change. Pin conflicts use last server-accepted operation wins. | The user wants local-first immediacy and clear semantics. Coupling Pin to Favorite or blocking pending moments would make the feature feel inconsistent with existing local-first editing behavior. | Yes | human |
| D116 | Pinned Moments grill-me 2026-05-08 | verification | Pinned feature verification uses simulator UI checks plus isolated sync validation | For non-main worktree implementation, use simulator validation primarily for UI and interaction logic: header, expansion preference, more-than-three sheet, sheet detail navigation, and context menus. Prove sync/data correctness with focused tests and isolated `3211` server/data smoke. Do not install the feature worktree build to the real iPhone by default. | The user wants feature-worktree validation to avoid overwriting the daily iPhone app while still proving the visible behavior. Simulator UI validation is not a substitute for sync tests, so both gates are required. | Yes | human |
| D117 | Post-M011 triage 2026-05-08 | roadmap | Sync Doctor is the only newly accepted next feature direction from this triage | Introduce Sync Doctor as a future diagnostics slice that turns Sync Health evidence into a guided diagnosis and safe next action. Do not pursue App Lock / Privacy Mode or human-readable archive export. Existing startup/capture flow is good enough, so Shortcuts / Action Button optimization is not needed now. Monthly/Custom Review, local semantic search, Web Clip, and Mac Server Watchdog are recorded as not considered for now. Export/import remains migration-first rather than human-readable. | The roadmap should protect the current quiet, local-first product shape and focus near-term work on the issue that has repeatedly cost debugging time: understanding sync/network/media health without turning Timeline or Admin into a dashboard. | Yes | human |
| D118 | Stronger Markdown discussion 2026-05-08 | composer-edit-markdown-ux | Stronger Markdown editing is a candidate, not an implementation decision yet | The user wants to consider stronger Markdown editing beyond the current H1/H2-only body editor. This does not yet supersede D077/D078. Before implementation, define the exact supported syntax, editor controls, rendering surfaces, IME/long-text stability requirements, media behavior, and whether the feature still serves lightweight expression rather than a full writing tool. | The current editor deliberately stopped at H1/H2 after list rendering proved fragile. Expanding Markdown can be valuable, but only if the boundary is explicit enough to avoid UI clutter, sync/search surprises, or unstable CJK input behavior. | Yes | human |
| D119 | M012 Check-ins grill-me 2026-05-08 | checkins-product | Check-ins are a third primary iOS surface with independent entries | Add `Check-ins` as a bottom tab after `Calendar`, while app launch still defaults to `Timeline`. Check-in entries are independent local-first facts, not ordinary Moments; Timeline directly renders entries whose `showInTimeline` is true instead of creating linked post rows. | The user wants check-ins to stand beside Timeline and Calendar, and wants Timeline visibility to be a per-entry display choice independent from whether the check-in exists. | Yes | human |
| D120 | M012 Check-ins grill-me 2026-05-08 | checkins-calendar-review | Calendar and Review use check-ins as life-activity signals independent of Timeline visibility | Calendar heatmap counts each non-deleted check-in entry as one activity unit alongside moments. Day Review shows all check-ins for the day, Month Stats splits `Moments` and `Check-ins` plus per-item counts, and Weekly Review may use item/date/count structure signals without reading notes/media. | Calendar and Review should represent lived activity, not only Timeline publication. The user explicitly separated publish visibility from check-in truth. | Yes | human |
| D121 | M012 Check-ins grill-me 2026-05-08 | checkins-ux | Daily check-in use is one-tap first | The Check-ins tab defaults to `Today`; tapping an item main area immediately records without a prompt. Notes/media use a separate lightweight content entry. Once-per-day items sink after completion; multiple-per-day items stay in manual order; off-schedule items live in a folded section but temporarily appear in Today after recording. | The feature must stay faster than using tags or creating ordinary moments, while still supporting richer entries when the user deliberately asks for content. | Yes | human |
| D122 | M012 Check-ins grill-me 2026-05-08 | checkins-data | Check-in items define recurrence and entry identity | Items include name, icon/color, record mode, weekdays, manual order, default Timeline visibility, and optional Smart Tag defaulting to none. Entries include item, occurredAt, optional note/media, entry-level Timeline visibility, and soft-delete state. Once-per-day uniqueness is item plus local date, and edits must revalidate the target item/date. | The user needs both one-a-day activities and multi-entry activities like meals, with later correction of date/item while preserving local-first facts. | Yes | human |
| D123 | M012 Check-ins grill-me 2026-05-08 | checkins-ai-media-tags | Check-in media and tags stay controlled and non-AI | Check-in media stores/syncs/plays but does not run transcription, AI summary, OCR, or AI tags. Item tags are optional, default none, inherit to all historical entries, and remain secondary metadata; check-in row identity uses item icon/color before tag color. | Check-ins already carry structure through the item. Running AI or making tags prominent would make the feature noisier and less predictable than the intended quick activity record. | Yes | human |
| D124 | M012 Check-ins grill-me 2026-05-08 | checkins-settings-admin | Check-ins management and diagnostics are iOS-first | Item/entry management lives on iOS. Mac Admin does not manage Check-ins in v1. Check-in sync/media diagnostics belong in iOS Settings diagnostics; Mac Admin remains limited to Mac-local recovery, archive, logs, process, filesystem, and maintenance operations. | The owner wants routine product and diagnostic work on the phone, while Mac Admin continues shrinking toward low-frequency machine operations. | Yes | human |
| D125 | M012 Check-ins grill-me 2026-05-08 | checkins-scope | Check-ins v1 avoids habit-tracker pressure features | Do not implement reminders, missed entries, streaks, completion rates, count goals, comments, favorite, pin, preset templates, Mac Admin editing, separate export, or conflict-resolution UI for Check-ins v1. | The module should record private life activity without becoming a KPI dashboard, task system, or second Moment model. | Yes | human |
| D126 | M012 check-in media follow-up 2026-05-08 | checkins-media-sync | Check-in photos use independent media ownership | Check-in image media is parented by `checkin_entry` through `checkin_media` / `local_checkin_media`, uploads through `/api/v1/checkin-media/upload`, recovers through `/api/v1/checkin-media/batch-download`, and deletes with `delete_checkin_media`. It must not create ordinary posts or ordinary post media. Audio/video check-in media remains disabled for this checkpoint, though the UI may reserve future affordances. | Meal-style check-ins need a photo path, but Timeline publication remains a per-entry display choice. Independent media ownership preserves the product boundary and keeps check-in media out of AI summary, transcription, OCR, and AI tag pipelines. | Yes | agent |
